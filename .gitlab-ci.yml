pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r docs/_build/html/* .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - proposal_definitions
    - master
